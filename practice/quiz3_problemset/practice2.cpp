//(a) state the heap order property for a min heap

// Every parent node has a key less that or equal to each of its children

//(b)

// Yes. Swapping the left and right subtrees does not violate the heap-order property or the complete binary
// tree property. The root remains less than or equal to both children, and each subtree is still a valid min heap

