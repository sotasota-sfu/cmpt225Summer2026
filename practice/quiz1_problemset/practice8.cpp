//(a) insertion sort on n integers
// A. O(n^2)

//(b) insertion sort on 5n integers
// A. O((5n)^2) = O(25n^2) = O(n^2)

//(c) min of singly list
// O(n)

//(d) Calculating a^n, where a and n are integers bigger than 1, and it does in the "obvious" method of repeated multiplication.
// O(n)

//(e) Calculating an, where a and n are integers bigger than 1, and it does in the "repeated squaring" method(see section 4.2.6 of the textbook).
// O(logn)

//(f) Testing a doubly - linked list of n integers is in sorted order from smallest to largest.
// O(n)

// (g) Calculating the sum 1 + 2 + 3 + ...+ n using a loop.
// O(n)

//(h) Calculating the sum 1 + 2 + 3 +...+ n using recursion.
// O(n)

// (i) Calculating the sum 1 + 2 + 3 +...+ n using the formula n(n +1) / 2.
// O(1)
